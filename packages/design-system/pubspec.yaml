name: yugrow_design_system
description: Yugrow Design System — tokens, theme, and components for Flutter
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.1.0
